import React from "react";

function JsxExample() {
  return (
    <div>
      <h1>Introduction</h1>
      <p>
        JSX Stands for JavaScript XML and its special syntax is used in React to
        make building user interfaces easier. JSX allows you to write HTML-like
        code directly inside JavaScript. JSX looks like regular HTML, it’s
        actually a syntax extension for JavaScript.
      </p>
    </div>
  );
}

export default JsxExample;
